using Microsoft.AspNetCore.Authorization;

namespace medeixeApi.WebUI.Controllers
{
    [Authorize]
    public class ComplaintController : ApiControllerBase
    {
        //BREAD = browse read edit add delete
    }
}